"""Training entry points: COCO fine-tuning, cone models, and LR experiments.

Run with ``python -m objdetect.cli.training.<name>`` — for example
``python -m objdetect.cli.training.train --help``.
"""
